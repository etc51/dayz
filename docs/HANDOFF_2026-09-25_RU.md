# HANDOFF: «Глобальная Черноруссия [RU]» — 25.09.2026

> Составлено по фрагменту треда, который пользователь вставил в облачную сессию. Тред неполный, остальное дополняется.

## Где что лежит
- Рабочая папка проекта (ПК пользователя): `C:\Users\HONOR\Documents\ChatGPT\дейз мод`.
  Не создавать копию проекта, не терять исходники, артефакты и журналы.
  Перед работой читать местный `AGENTS.md`.
- Репозиторий: https://github.com/etc51/dayz — синхронизация облака и ПК.
- Workshop: «Глобальная Черноруссия [RU]», ID **3806148351**, автор etc51.
- Сайт: https://global-chernarus.vercel.app — пока показывает старые инструкции.

## Цель сейчас
Обновить существующий RU-мод 3806148351 тестовой сборкой v15, чтобы Максим
скачал её и проверил сервер и игру.
- Новый мод не создавать, EN-версию не публиковать.
- «Чёрный рассвет» входит в тот же RU-мод, отдельно не предлагать.
- Полную работоспособность не заявлять: это тестовая сборка.

## Пакет для загрузки
- Загружать ровно `C:\DZT\gcru-v15-20260925\payload\@GCWorldRU`:
  64 файла, 114 005 827 байт. Это 2 подписанных RU PBO, подписи, публичный ключ,
  `mod.cpp`/`meta.cpp` и `server-bundle-ru` (миссия, серверные файлы, инструкции).
- Квитанция пакета, SHA-256 `705a1314b9b4ca18256d96bc8e3e3696d1bdd2f8e2290ea05f595f6c7578aeb3`, статус `PREPARED_NOT_UPLOADED`.
- Не загружать корень `C:\DZT\gcru-v15-20260925` и `world-signed/@GCWorldRU`:
  во втором нет `server-bundle-ru`.
- Исходная подписанная v15 (`world-signed`), квитанция SHA-256
  `bd25d66ab3fe9ee10a337b615b2c6ba14bf3a18cf7e91db3281cecf9fbab7140`:
  - `gc_world_ru.pbo` — `cc2fb688686d741421dabb08a7d985c81b18c1d06c0a3fbd144f8d76a3bf4b9d`
  - `black_dawn_campaign_ru.pbo` — `2cae8645bdcdba3f74c970f134a2fc8225910c5043bf1284fc852dc9282a48bc`
- Описание: `C:\DZT\gcru-v15-20260925\pages\ru.json` и `description.txt`.
  Первой идёт ссылка на сайт. Версия названа тестовой, установка ручная (Ubuntu/Windows),
  10 зависимостей ссылками, такси и рынок выключены до игровой проверки.
  Автоматический установщик не обещать: от скриптов отказались.

## Зависимости (порядок в `-mod`, наш мод — последним; не в `-serverMod`)
1. CF `1559212036`
2. Dabs Framework `2545327648`
3. Expansion Core `2291785308`
4. Expansion Book `2572324799`
5. Expansion Navigation `2792984722`
6. Expansion Groups `2792983364`
7. Expansion Quests `2828486817`
8. Expansion AI `2792982069`
9. Expansion Market `2572328470`
10. Expansion Chat `2792982897`
11. **@GCWorldRU** (3806148351)

## Что проверено, а что нет
- Прогон v15 на Windows: модули GameLib/Game/World/Mission загрузились без `SCRIPT (E)`,
  в журнале дошло до `[BlackDawn] CONFIGS_LOADED ... quests=21 npc_data=3`.
- Затем процесс остановила защита по памяти: свободно 0,744 GiB при пороге 0,9 GiB.
  Сервер занимает ~3,5 ГБ при ~4 ГБ свободных. Маркеров полной готовности нет.
- Проверялась копия штатной миссии, а не полный `gcWorld.chernarusplus` из server-bundle.
- **Не подтверждено:** вход клиента, Ubuntu, квесты, NPC, сохранения, игровой путь.
- Вердикт: `ALLOW TEST BUILD`, `BLOCK RELEASE`.
- Лексическая проверка 414 скриптов v15 — без находок.
- Известный опасный паттерн: массивы через `= {}` вместо `new` (из-за него раньше
  падал сервер). Найден в правках радио от Codex, отправлен на исправление.

## Ошибки у игроков
- Максим (Ubuntu) присылал много ошибок компиляции старых PBO. Лишний
  `./addons/gc_world_ru.pbo` в корне сервера удалили, но ошибки остались,
  так что это не только проблема установки.
- cryomanne: `gcservicecustodyruhand.c(16): Expected ')', not a 'DROP'` — ошибка старого
  опубликованного PBO (разбор: `docs/GLOBAL_CHERNARUS_DROP_REVIEW_20260924_RU.md`),
  файла в исходниках больше нет.
- Максима не просить повторять старые ручные патчи и не просить удалять сервер или сохранения.
  После публикации сначала убедиться, что он скачал новые файлы.

## Почему Steam ещё не обновлён
- Фоновый загрузчик DayZ Tools: 32-битный SDK вернул `SteamNotRunning`.
- DayZ Publisher 25.09.2026 10:06: `Steam online: False`. Кнопку Update не нажимали,
  нового Manifest ID нет. **Не утверждать, что Steam обновился.**
- Вход в Steam, пароль и Steam Guard вводит только пользователь.

## Следующие шаги
1. Пользователь подтверждает, что Steam в сети и можно управлять Publisher.
2. Перепроверить квитанцию пакета и текущую страницу 3806148351.
3. Загрузить `payload\@GCWorldRU` в существующий item 3806148351 с `pages\ru.json`
   (Update — только после второго подтверждения пользователя).
4. Сверить публичное описание и, если получится, хэши PBO в свежескачанной версии из Workshop.
   Только после этого писать Максиму. Работоспособность игры остаётся открытым вопросом до его теста.
5. Отдельно обновить сайт global-chernarus.vercel.app: там ещё обещаны `GC-Server-Ubuntu.sh`/`INSTALL.cmd`.

## Последний запрос пользователя (до лимита)
«Для начала полностью проверь код проекта на ошибки и попробуй запустить сервер на Windows».
Было начато: статический аудит v15, подготовка отдельного экземпляра сервера в структуре
Максима (`@GCWorldRU` последним в `-mod`, миссия `gcWorld.chernarusplus`).
Полный аудит проекта в фоне упал. Нужна машина с запасом RAM ≥ 5 ГБ свободных.

## Проверено из облака 25.09.2026 ~16:00 UTC
- Сайт global-chernarus.vercel.app отвечает 200 (Vercel, США), уже с новыми инструкциями
  (ручная установка, автоустановщик снят). Вероятная причина «не открывается» у пользователя —
  блокировка *.vercel.app в России. Зеркало: папка `site/` → GitHub Pages (`.github/workflows/pages.yml`).
- Workshop 3806148351 обновлён: `time_updated` 2026-09-25T14:34:40Z (17:34 МСК), видимость публичная, не забанен,
  описание новое (тестовая версия, ручная установка, 10 зависимостей, такси отменены, рынок выключен).
- `file_size` в Steam = 113 943 545 байт, а пакет payload = 114 005 827 байт (разница 62 282).
  Нужно на ПК проверить, какая папка была загружена: payload\@GCWorldRU или world-signed\@GCWorldRU
  (во втором нет server-bundle-ru). Хэши PBO из Workshop ещё не сверены.
